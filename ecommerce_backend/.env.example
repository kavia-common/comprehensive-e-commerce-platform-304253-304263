# Optional (recommended for deployments):
# If not set, the backend will attempt to read the monorepo database connection file:
#   comprehensive-e-commerce-platform-304253-304262/ecommerce_database/db_connection.txt
# and finally fallback to local dev default: postgresql://appuser:dbuser123@localhost:5000/myapp
DATABASE_URL=postgresql://appuser:dbuser123@localhost:5000/myapp
